working_dir: ./

excludes:
  - ".git/"

env_vars:
#  VLLM_USE_V1: "1"
  DEBUG_NO_PADDING_2_PADDING: "1"
  HYDRA_FULL_ERROR: "1"
  NCCL_NVLS_ENABLE: "0"
  NCCL_SOCKET_IFNAME: "eth0"
  TMPDIR: "/tmp"
  CUDA_HOME: "/usr/local/cuda"
  CUDA_TMPDIR: "/tmp"
  CUDA_CACHE_PATH: "/tmp/cuda_cache"
  # For distributed training, the path must be set on a distributed file system (DFS) to ensure visibility across all nodes.
  HF_HOME: "/tmp/hf_home_mimo"
  PYTHONPATH: "/tmp/hf_home_mimo/modules/"
  TENSORBOARD_DIR: "/home/hadoop-djst-algoplat/data/tensorboard/qwen2.5-7b-math/4-4-dapo-use_trainer_do_validate_0507_vllm"
  MODEL_PATH: "/home/hadoop-djst-algoplat/model/Qwen2.5-0.5B-Instruct"
#  MODEL_PATH: "/home/hadoop-djst-algoplat/model/Qwen2.5-Math-7B"
  TRAIN_FILE: "/home/hadoop-djst-algoplat/data/gsm8k/train.parquet"
  TEST_FILE: "/home/hadoop-djst-algoplat/data/gsm8k/test.parquet"
  http_proxy: "http://10.177.8.11:3128"
  https_proxy: "http://10.177.8.11:3128"
  # Do NOT use VLLM_ATTENTION_BACKEND - it is not a valid vLLM v1 env var.
  # To work around fa2 PTX incompatibility on this cluster, use enforce_eager=True
  # in the rollout config and set attention_backend via engine_kwargs.vllm instead.
#  NCCL_CUMEM_ENABLE: "0"
#  NCCL_CUMEM_HOST_ENABLE: "0"
#  ROLLOUT_NAME: "vllm"